node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# local env (key also lives as a fallback in src/config.js so the app still runs)
.env

# design handoff reference (kept out of the app build)
_design_ref
